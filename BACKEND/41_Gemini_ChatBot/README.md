# BEE-eval-1

### git clone this repository 

### Install the required dependencies to run this project, run the following command in your terminal:

```bash
echo "Installing project dependencies..."
npm install express body-parser fs path morgan helmet cors compression cookie-parser

echo "All dependencies installed successfully!"
```
